<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Techblog</title>
  {{!-- reset and bootstrap --}}
  <link rel="stylesheet" href="/css/reset.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <section class="flex-column justify-space-around">
    <header class="display-flex justify-space-between align-center bg-info p-2">

      <nav class="row text-white p-4">
        {{!-- <!-- Conditionally render login or logout links --> --}}
        <section class="col-1 text-center"><a href="/" class="text-white text-decoration-none h4">Home</a></section>
        <section class="col-1"><a href="/dashboard" class="text-white text-decoration-none h4">Dashboard</a></section>
        <section class="col-1 text-end">
          {{#if logged_in}}
          <a class="text-white text-decoration-none h4 pointer" id="logout">logout</a>
          {{else}}
          <a href="/login" class="text-white text-decoration-none h4 pointer">login</a>
          {{/if}}
        </section>
      </nav>
      <section class="row">
        <h2 class="col-3 text-center fw-bold text-white mb-4">Tech Blog</h2>
      </section>
    </header>
    <main class="container container-fluid mt-5">

      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </section>

  <!-- load logout script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
